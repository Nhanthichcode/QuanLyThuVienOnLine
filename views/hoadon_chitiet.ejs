<!DOCTYPE html>
<html lang="vi">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= title %></title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css"
      rel="stylesheet"
    />
  </head>
  <body>   
      <%- include('navbar') %>
       <div class="container mt-4">
      <div class="card">
        <div class="card-header bg-primary text-white">
          <h3 class="mb-0"><i class="bi bi-receipt"></i> <%= title %></h3>
        </div>
        <div class="card-body">
          <div class="mb-3 soluong">
            <strong >Người dùng:</strong> <%= hoaDon.taiKhoanID?.HoVaTen ||
            hoaDon.taiKhoanID?.TenDangNhap || 'N/A' %>
          </div>
          <div class="mb-3 soluong">
            <strong >Ngày tạo:</strong> <%= hoaDon.ngayTao ? new
            Date(hoaDon.ngayTao).toLocaleDateString("vi-VN") : '' %>
          </div>
          <div class="mb-3 soluong">
            <strong >Tổng tiền:</strong>
            <span class="text-danger fw-bold"
              ><%= hoaDon.tongTien.toLocaleString("vi-VN", { style: "currency",
              currency: "VND" }) %></span
            >
          </div>
          <h5 class="mt-4">Danh sách sản phẩm</h5>
          <div class="table-responsive">
            <table class="table table-bordered table-hover align-middle">
              <thead class="table-light">
                <tr>
                  <th>Tên sách</th>
                  <th>Số lượng</th>
                  <th>Giá bán</th>
                  <th>Thành tiền</th>
                </tr>
              </thead>
              <tbody>
                <% hoaDon.danhSachSanPham.forEach(item => { %>
                <tr>
                  <td><%= item.sachId?.TieuDe || 'N/A' %></td>
                  <td align="right"><%= item.soLuong %></td>
                  <td align="right">
                    <%= item.giaBan.toLocaleString("vi-VN", { style: "currency",
                    currency: "VND" }) %>
                  </td>
                  <td align="right">
                    <%= (item.soLuong * item.giaBan).toLocaleString("vi-VN", {
                    style: "currency", currency: "VND" }) %>
                  </td>
                </tr>
                <% }) %>
              </tbody>
            </table>
          </div>
          <a href="/hoadon" class="btn btn-secondary mt-3"
            ><i class="bi bi-arrow-left"></i> Quay lại</a
          >
        </div>
      </div>
      <%- include('footer') %>
    </div>
    </div>
    <%- include('javascript') %>
  </body>
</html>
